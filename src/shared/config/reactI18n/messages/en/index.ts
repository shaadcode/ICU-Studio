import enCommonMessages from './common.json';
import enEditorMessages from './editor.json';
import enProjectsMessages from './projects.json';

const enMessages = {
  common: enCommonMessages,
  editor: enEditorMessages,
  projects: enProjectsMessages,
};

export default enMessages;
