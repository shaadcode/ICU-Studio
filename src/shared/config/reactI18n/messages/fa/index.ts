import faCommonMessages from './common.json';
import faEditorMessages from './editor.json';
import faProjectsMessages from './projects.json';

const faMessages = {
  editor: faEditorMessages,
  common: faCommonMessages,
  projects: faProjectsMessages,
};

export default faMessages;
